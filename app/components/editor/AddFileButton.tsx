export const AddFileButton = () => {
  const handleAddFile = async () => {
  }

  return (
    <button onClick={handleAddFile} className="mt-4 rounded border dark:border-smoke-500 bg-slate-500 dark:bg-smoke-600 px-3 py-[0.35rem] font-semibold text-sm text-slate-50 hover:bg-blue-700">
      Add file
    </button>
  )
}
